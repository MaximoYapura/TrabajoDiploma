using System;

namespace MPP_08YS
{
    // OBSOLETO / NO UTILIZADO: SqlClienteRepository_790MY mapea directamente
    // desde SqlDataReader (no usa DataTable), por lo que este Mapper quedó
    // huérfano y su comportamiento nunca se ejecuta. Se deja este marcador
    // en lugar de borrar el archivo porque no hay una herramienta de borrado
    // de archivos disponible en esta sesión; se recomienda eliminarlo desde
    // Visual Studio.
    [Obsolete("No usado por SqlClienteRepository_790MY. Candidato a eliminación manual.")]
    public static class ClienteMapper_790MY_OBSOLETO
    {
    }
}
